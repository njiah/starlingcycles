name: starlingcycles
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.4.1 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  sqflite: ^2.3.3+1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_bloc: ^8.1.6
  equatable: ^2.0.3
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  assets:
    - images/
